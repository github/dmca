name: Validate Filepaths Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: .github/script/validate-filepaths/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: .github/script/validate-filepaths

      - name: Run tests
        run: npm test
        working-directory: .github/script/validate-filepaths